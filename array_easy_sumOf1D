Running Sum of 1D Array
easy
Produce an array where output index i equals nums[0] + nums[1] + ... + nums[i].


class Solution:
    def runningSum(self, nums:list[int])->list[int]:
          res=[]
          for i in range(len(nums)):
              res.append(sum(nums[:i+1))
          return res
